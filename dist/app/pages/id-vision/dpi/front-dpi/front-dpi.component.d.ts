import { AlertController, LoadingController, ModalController } from '@ionic/angular';
export declare class FrontDpiComponent {
    private alertController;
    private loadingController;
    private modalController;
    constructor(alertController: AlertController, loadingController: LoadingController, modalController: ModalController);
}
